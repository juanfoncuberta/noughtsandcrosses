import { tint } from 'polished';

  const theme = {
    colors: {
        black: '#000',
        white: '#fff',
        primary: '#D67E45',
        blue: '#1E90FF',
        red: '#ff000f'
    }
};

/* const theme = {
  colors: {
    ...basicColors,
    grayDark: tint(0.8, basicColors.black),
  },
}; */

export default theme;
